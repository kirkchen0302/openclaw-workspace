# 2026-03-26

## audience_dashboard 定義修正（重要）

### 919 載具（載具驗證失敗）

**定義**：財政部回應代碼 919，代表該會員的手機載具驗證碼錯誤，無法同步發票資料。

**正確判斷方式**：
```sql
-- 正確（最終版）
JOIN (
  SELECT carrier_hk, is_active, mof_status,
    ROW_NUMBER() OVER (PARTITION BY carrier_hk ORDER BY effective_from DESC) AS rn
  FROM `production-379804.intermediate.sat__carrier`
) c ON lmc.carrier_hk = c.carrier_hk
WHERE c.rn = 1
  AND c.is_active = TRUE
  AND c.mof_status = 'INACCURATE_AVAILABILITY'
```

**欄位說明**：
- `is_active`：載具條碼是否存在（FALSE = 已被刪除，不納入）
- `mof_status`：財政部驗證結果，sat__carrier 有多筆歷史，需取最新一筆
- `INACCURATE_AVAILABILITY`：財政部回傳 919，驗證碼錯誤

**舊（錯誤）方式們**：
```sql
-- ❌ v1：barcode 格式判斷，完全錯誤
WHERE hc.barcode LIKE '/%'

-- ❌ v2：沒取最新一筆，且 is_active 邏輯搞反
WHERE c.mof_status = 'INACCURATE_AVAILABILITY'

-- ❌ v3：is_active=FALSE 是「已刪除載具」，不是 919
WHERE c.is_active = FALSE
```

---

### 活躍使用者定義

**定義**：近 30 天內有 `session_start` event 的會員視為「活躍」。

- 依據：`base__sat__session_session_start_activity` table
- 不參考其他 event（如 page_view、purchase 等）
- 快照基準日：`2026-03-22`（固定時間點，不隨日期變動）

---

### update_dashboard.py 修正紀錄

- 修正日期：2026-03-26
- commit：`fix: 919 判斷改用 mof_status=INACCURATE_AVAILABILITY，移除錯誤的 barcode LIKE 判斷`
- 檔案：`/Users/kirk/.openclaw/workspace/docs/update_dashboard.py`
- 影響：四群 KPI card 中「919載具」人數將重新計算，舊 HTML 資料為錯誤值

---

### audience_dashboard 現況

- **位置**：`/Users/kirk/.openclaw/workspace/docs/audience_dashboard.html`
- **GitHub Pages**：已部署（透過 git push 自動更新）
- **每日更新腳本**：`docs/update_dashboard.py`
- **快照基準**：2026-03-22（固定）
- **趨勢起始**：2026-03-01
- **四群定義**：
  - `ios_active_30d`：iOS，快照基準前 30 天內有 session_start
  - `ios_inactive_30d`：iOS，快照基準前 30 天內無 session_start
  - `android_active_30d`：Android，快照基準前 30 天內有 session_start
  - `android_inactive_30d`：Android，快照基準前 30 天內無 session_start

## 用戶確認事項（Kirk 說明）
- `active` = 是否活躍，判斷依據是有 `session_start` event，不參考其他 event
- 919 = 財政部回應代碼，代表手機載具驗證碼錯誤（對應 `mof_status = 'INACCURATE_AVAILABILITY'`）
- 「919 使用者」無法同步發票資料回來
